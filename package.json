{
  "name": "mkugmk-web-app",
  "version": "1.0.0",
  "description": "Repo for mkugmk website",
  "author": "Vlad Bragin",
  "scripts": {
    "start:web": "npm start --prefix ./web",
    "start:server": "npm start --prefix ./server",
    "dev": "concurrently --kill-others \"npm run dev --prefix ./web\" \"npm run develop --prefix ./server\"",
    "dev:web": "npm run dev --prefix ./web",
    "dev:server": "npm run develop --prefix ./server",
    "build:web": "npm run build --prefix ./web",
    "build:server": "npm run build --prefix ./server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
