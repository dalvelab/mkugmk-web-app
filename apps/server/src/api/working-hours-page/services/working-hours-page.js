'use strict';

/**
 * working-hours-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::working-hours-page.working-hours-page');
